{
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run frontend\"",
    "server": "cd back && npm run back",
    "frontend": "cd frontend && npm run dev"
  }
}
